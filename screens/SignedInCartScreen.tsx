import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SignedInCartScreen = () => {
  return (
    <View>
      <Text>cart</Text>
    </View>
  );
};

export default SignedInCartScreen;

const styles = StyleSheet.create({});
